<Page
    x:Class="SaveSavings.View.DetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SaveSavings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <Button x:Name="BackButton" Click="Back_Click" Style="{StaticResource NavigationBackButtonNormalStyle}"/>
            <TextBlock Text="Delete/Update contact" Style="{ThemeResource HeaderTextBlockStyle}" Margin="12,0" />
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1" Name="EditView">
                <DatePicker x:Name="w_DateOfExpense" Header="Date" Margin="10"/>
                <TextBox x:Name="w_AmountOfExpense" Header="Amount" InputScope="CurrencyAmount" MaxLength="10" />
            </StackPanel>
            <StackPanel Grid.Row="2">
                <Button Content="Delete" HorizontalAlignment="Stretch" Margin="10" Click="DeleteContact_Click"/>
                <Button Content="Update" HorizontalAlignment="Stretch" Margin="10" Click="UpdateContact_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
    
</Page>
